import { supabase, isSupabaseConfigured, supabaseUrl, supabaseAnonKey } from '../services/supabase';

export { supabase, isSupabaseConfigured, supabaseUrl, supabaseAnonKey };
export default supabase;

